import { TransFormInterceptor } from './trans-form.interceptor';

describe('TransFormInterceptor', () => {
  it('should be defined', () => {
    expect(new TransFormInterceptor()).toBeDefined();
  });
});
